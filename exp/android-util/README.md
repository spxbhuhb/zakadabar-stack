Miscellaneous Android utilities.

The [SQLDroid](https://github.com/SQLDroid/SQLDroid/) package is a port of the 
SQLDroid project to Kotlin. This port was necessary at the time because Exposed
depends on the name of the driver and it is hard coded on both sides.